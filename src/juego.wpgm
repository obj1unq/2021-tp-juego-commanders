import wollok.game.*
import enemigos.*
import configuraciones.*
import ataques.*

//Main
program juego {
	game.title("commanders")
	game.height(50) //alto
	game.width(140) //ancho
	game.boardGround("fondo.png")
	game.cellSize(10) // ajustar el tamaño de pixeles de celda	
	nivel1.iniciar()
	game.start()
}